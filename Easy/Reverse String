class Solution {
public:
    void reverseString(vector<char>& s) {
        int i, j;
        for(i = 0,j = s.size()-1; i<j; j--,i++)
        {
            swap(s[i],s[j]);
        }
        
    }
};
